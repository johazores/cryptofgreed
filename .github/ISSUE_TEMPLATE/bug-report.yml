name: Bug report
description: Report a reproducible defect in the current prototype
title: "fix: "
labels:
  - bug
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting a bug. Do not include secrets, private keys, production credentials, or exploitable vulnerability details. Use the Security tab for sensitive reports.
  - type: textarea
    id: summary
    attributes:
      label: What happened?
      description: Describe the defect and the expected behavior.
      placeholder: The current behavior is... I expected...
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Reproduction steps
      description: Provide the smallest reliable sequence that reproduces the problem.
      placeholder: |
        1. Sign in as...
        2. Open...
        3. Select...
        4. Observe...
    validations:
      required: true
  - type: input
    id: route
    attributes:
      label: Affected route or feature
      placeholder: /dashboard/game/[characterId] or merchant room
    validations:
      required: true
  - type: dropdown
    id: frequency
    attributes:
      label: Frequency
      options:
        - Every time
        - Often
        - Sometimes
        - Once
    validations:
      required: true
  - type: textarea
    id: environment
    attributes:
      label: Environment
      description: Include browser, operating system, device size, Node version, and relevant local services.
      placeholder: Chrome 150, Windows 11, 1440x900, Node 22
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: Sanitized logs or screenshots
      description: Remove credentials, tokens, personal data, database values, and private identifiers.
  - type: checkboxes
    id: checks
    attributes:
      label: Reporter checklist
      options:
        - label: I searched existing issues for the same defect.
          required: true
        - label: I removed secrets and personal data from this report.
          required: true
        - label: This is not a sensitive security vulnerability.
          required: true
